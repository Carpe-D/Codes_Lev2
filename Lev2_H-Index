def solution(citations):
    h=0
    count=0
    while(True):
        for i in range(0,len(citations)):
            if(citations[i]>=h):
                count+=1
        if (count>h):
            count=0
            h=h+1
        
        else:
            if(count<h):
                h=h-1
            return h
